# 专业术语最终范围复核 — 2026-09-16

## 结论

按作者最新确认的参考分工，已完成本轮术语双向核对。**在所检查版本的英文摘要、各章正文、活动表格与文字图题中，排除他人负责的B类及作者明确排除的图内旧名称后，未发现需要新增修改的专业术语问题；本轮没有未裁决的新术语候选。**

这是有版本和范围的审查结论，不是绝对零遗漏保证，也不是语法、全部引文原始论文、技术方法或数值正确性的全面认证。本轮没有修改论文。

## 最新参考分工

- JE：特征工程、HI提取与筛选。
- BMS：卷积、注意力、局部—全局建模，以及计算与资源指标。
- SL：轻量化表达。
- 三篇可以相互对照；主参考不是排他规则。不因主参考变更自动推翻作者已批准且含义合适的词汇。

## 本轮怎样检查

1. 固定 `build/terminology-closure-20260916-153549/` 快照，递归记录实际输入链。包含6份章节、18份活动表、11份图/共用布局文字，以及主文件、格式与参考文献依赖；没有把未引用历史表当当前论文。
2. 复用刚完成的全文通读，同时新增反向对照：先从范文功能对应完整段落提取专业对象，再回查本文各章同对象用法，复读命中上下文和中文定义。并非只是搜索旧问题的替换词。
3. 分别完成23类HI对象、21类模型对象、20个资源/实验检索词族的核对记录。三份记录有重叠，不能相加声称为64个独立专名；检索词族也不是错误数量。
4. 每个对象记录本文位置/变体、范文定位、是否同对象，以及已对齐、合理不同、B类排除或新待核状态。对不同对象不强制同词；对全文已经固定且范文支持的词不反复更换近义词。
5. 以“技术对象＋出现位置＋问题实质”去重。已完成A1—A8与补充6处不重报；B1—B8由他人负责；旧保留项没有新证据不重启。
6. 提交前对全部快照依赖做哈希比较，结果没有快照后变化。因此本轮结论无需对新增并行修改作额外推断。

本轮由两名agent分别完成HI与模型方向反向核对，主代理完成资源/实验方向、去重和版本检查。三份记录说明复用阅读与新增阅读的区别；没有声称三人各自独立逐字重读三篇范文全文。

## 专业词的具体保留判断示例

- `locality bias`：SL前置小核卷积的对应语境已有原词，本文无需另换成 `inductive bias`。
- `moving average`、`window size`、取秩与HI子集：与JE的功能语境及本文定义一致，无新增版本冲突。
- `feature embedding dimension` 与 `embedding dimension`：限定与简称，不是新维度。
- `multi-channel features` 与 `cross-channel feature fusion`：分别指特征和融合操作，不能机械合并。
- `root mean square error`：BMS写作 `Root Mean Squared Error`，JE与SL采用 `Root Mean Square Error`；当前词有范文依据且已确认，不制造新问题。
- `FLOPs`、`training time`、可训练参数量及 `storage size`：BMS相应测量定义可直接对照；不把单次前向运算改成整个训练运算，不把权重文件大小改成实测运行内存或Flash占用。
- 自有RAA及HI定义：范文提供表达依据，不提供必须照搬的机制名称。没有因三篇范文不同而要求本文新增门控、改变agent数量或替换指标。

## 可追溯记录

- `build/terminology-closure-20260916-153549/hi-coverage.md`：23类HI对象及章级覆盖。
- `build/terminology-closure-20260916-153549/model-coverage.md`：21类模型对象及章级覆盖。
- `build/terminology-closure-20260916-153549/resource-coverage.md`：轻量化与计算/资源指标依据及裁决。
- 同目录 `resource-occurrences.json`：资源/实验词族在活动文本中的位置；`manifest.json`、`final-version-check.json`：版本证据。

## 收口条件

本次范围内的专业对象已经给出状态；双向对照完成；新增候选去重完成；新待核项为0；版本检查无变化。这些条件支持上述范围明确的最终结论。后续只有新增文字、改动技术对象或出现新的相反证据时，才需重开对应条目，不为反复查漏继续制造同义词替换。
