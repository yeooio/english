# LLGFA 名称同步：本次正文与待处理项

作者决定采用 **Linear Local-Global Fusion Attention (LLGFA)**，替代英文工作稿中的 **Slim Local-Global Fusion Attention (SLFA)**。本次仅同步 `chapters/abstract.tex`、`chapters/chapter01.tex`、`chapters/chapter03.tex` 的可见英文名称与缩写，包括第3章公式中的可见 `\operatorname{LLGFA}`。内部 `\label` 键保持不变；冻结中文源稿不修改。其他摘要措辞仍按逐句确认记录另行核对，不计入本次名称同步。

## 待同步备选；本次不修改

- `tables/table_4_4.tex` 第12行：`SLFA module`。作者将来处理表格。
- `figures/figure_3_3.tex` 图注：`SLFA module`，应在图像修订时与图中名称一并同步。
- `figures/01.png`：可见 `SLFA` 和 `SLFA features`，需在图像修订时改名。
- `figures/fig3_3_attention_comparison.png`：图(d)可见 `Skim Local-Global Fusion Attention`。修图时应同时核对其 DSConv-L 位置、局部分支和 RAA 分支与第3章公式所述数据流，不能仅改字。

历史审校记录保留旧名作为当时状态，不作批量重写。
